{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for every app window. Every window is created at runtime under a w<n> label: the configured window carries create: false, so nothing is labelled main any more",
  "windows": ["w*"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-close",
    "opener:default",
    "dialog:default",
    "store:default",
    "updater:default",
    "process:allow-restart"
  ]
}
